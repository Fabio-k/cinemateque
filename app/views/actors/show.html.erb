<h1><%= @actor.name%></h1>
<p><%= @actor.nacionality %></p>
<%= button_link 'Adicionar papel', new_actor_role_path(actor_id: @actor.id)%>
<ul>
    <% @actor.roles.each do |role|%>
        <li><%= role.name %> - <%= role.movie.title%></li>
    <% end %>
</ul>